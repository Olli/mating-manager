<div class="nested-fields">
   <%= f.inputs do %>
    <%= f.input :amount %>
    <%= f.input :mating_unit_id,
                    as: :select,
                    collection: MatingUnit.all.collect { |mu| [mu.name, mu.id] } %>
    <%= link_to_remove_association t('places.remove_place'),
                                   f,
                                   class: 'btn btn-danger'
     %>

  <% end %>
</div>