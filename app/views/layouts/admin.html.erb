<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title>Admin - MatingManager</title>
    <meta charset="utf-8" />
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    -->
    <%= stylesheet_link_tag 'application.css', :media => 'screen, projection' %>
    <%= javascript_include_tag :application %>
    <%= csrf_meta_tag %>
  </head>
  <body>
    <header>
      <span>
        <%= t(:headertext) %>
      </span>
    </header>
    <div id="container">
      <%= render :partial => 'admin/shared/logged_in' %>
      <%= render :partial => 'admin/shared/nav' %>
      <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <% end %>
      <div id="content">
        <%= yield %>
      </div>
    </div>
    <footer>
    </footer>
  </body>
</html>
